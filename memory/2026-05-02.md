# 2026-05-02

## Railway + Supabase Deployment (FINALLY WORKING!)

- **Problem:** Tasks weren't saving on dashboard refresh
- **Root cause:** Tasks table schema used different column names than our code expected
- **Schema discovered:** content (not title), status (not column), priority (integer), assigned_to and tags columns don't exist
- **Fix:** Rewrote index.js to map:
  - title → content
  - column → status
  - Removed assigned_to and tags (not in schema)
- **Verification:** Created test task directly via Supabase REST API - appeared on dashboard!
- **Status:** GET working, POST needs UUID format for IDs (dashboard sends string IDs)

## Dashboard URL
- https://mission-control-production-c38d.up.railway.app

## Supabase Tables
- clients: id (UUID), business_name, owner_email, created_at
- tasks: id (UUID), client_id, agent_id, content, status, priority, created_at, updated_at
- agents: id (UUID), client_id, name, role, status, config, created_at

## Realtime & Final Fixes

- **Supabase Realtime:** Enabled, added subscription in index.html to auto-refresh board on changes
- **Recursion fix:** Fixed initRealtime() calling init() causing infinite loop
- **Priority mapping:** Added priorityMap to convert 'low'/'medium'/'high' strings to integers
- **UUID validation:** Added regex check to generate UUIDs for timestamp-style IDs
- **Diagnostic layer:** Added global error handler that auto-creates HIGH priority task on 500 errors

## SYSTEM_ARCH.md Created
- **File:** Documentation of Stable Ground architecture
- **Contents:** Tech stack, global maps (statusMap, priorityMap, reverseStatusMap), realtime fix, DB constraints, troubleshooting codes
- **Backup:** Created backup.sh script to auto-sync critical files (SYSTEM_ARCH.md, SOUL.md, USER.md, AGENTS.md, MEMORY.md) to GitHub

## Realtime Tests (LIVE)
- **Phase A Test:** Created task via API, moved through INBOX → IN_PROGRESS → DONE - persisted correctly
- **Anon Key Fix:** Changed from service_role to anon key (sb_publishable_...) for client-side realtime
- **System Heartbeat Test:** Created task, moved to REVIEW after 3 seconds - UI updated without manual refresh
- **Tags Column:** Added to Supabase schema (type: text[]) to support metadata

## Current Stack
- **Compute:** Railway (Express server)
- **Database:** Supabase (PostgreSQL) with Realtime enabled
- **Frontend:** Static HTML with Tailwind CDN + Supabase-js realtime subscription

## Test Results
- Created "Internalize soul.md" task via API
- Moved to DONE column - persisted correctly
- Realtime updates working

## Current Stack
- **Compute:** Railway (Express server)
- **Database:** Supabase (PostgreSQL)
- **Frontend:** Static HTML with Tailwind CDN
- **Realtime:** Supabase Realtime subscription

## SYSTEM_ARCH.md Created
- **File:** Documentation of Stable Ground architecture
- **Contents:** Tech stack, global maps (statusMap, priorityMap, reverseStatusMap), realtime fix, DB constraints, troubleshooting codes
- **Backup:** Created backup.sh script to auto-sync critical files (SYSTEM_ARCH.md, SOUL.md, USER.md, AGENTS.md, MEMORY.md) to GitHub

## Final Realtime Fixes (2026-05-02 late)
- **Issue:** Realtime wasn't updating UI
- **Fix 1:** Changed from service_role to anon key (sb_publishable_...)
- **Fix 2:** Added window.onload wrapper to prevent load-order issues
- **Fix 3:** Moved Supabase SDK to <head> for early loading
- **Fix 4:** Added safety check for window.supabase.createClient
- **Testing:** Created tasks via API, moved through columns - persisted correctly
- **Final Verification:** Dashboard shows REALTIME_STATUS in console when subscribed
