VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Smart Import (Gemini 2.5 Flash) — required for the Recipe Builder's Smart Import card.
# Note: VITE_* vars are exposed to the browser. Use a key with restricted scope.
VITE_GEMINI_API_KEY=your-gemini-api-key

# Multi-store delivery tracking backend (Railway). Paste your live URL, no trailing slash.
VITE_API_URL=https://delivery-api-service.up.railway.app
